# Partner App Release

The partner application is a Flutter application intended for owners and marketers.

Production Android/iOS platform projects must be checked in and verified before store release. CI-only generation is a QA fallback, not a production configuration.

Signing credentials must remain outside Git and be injected through secure CI secrets.
